import React from 'react';
import {Text, View} from 'react-native';

const AllScreen = () => {
  return (
    <View style={{flex: 1}}>
      <Text>All Tasks</Text>
    </View>
  );
};

export default AllScreen;
